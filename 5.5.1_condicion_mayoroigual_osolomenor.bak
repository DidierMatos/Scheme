(printf "Función que recibe un número y retorna si el numero es mayor o igual a 10 o es solamente menor")

(define (numero n) 
  (cond ;utilizando cond, else es opcional de poner.
    [(>= n 10)(begin (display "Es mayor o igual que 10"))]
    [(< n 10)(begin (display "Es menor que 10"))]
    )
  )
    
(numero (read))