(printf " A) Funcion devuelva T o F si el numero es igual a 5")
(define (numero x)
  (< x  5))
(numero (read))
  #|(if (= x 5 )
      (print #t))
  (if (< x 5)
  (printf "continua"))
  (if (> x 5)
  (printf "continua"))
  )|#
  
